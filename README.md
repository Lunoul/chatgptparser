# LLM parser

## Simple parser folders and files names for good LLM prompts

    The function with .gitignore is not done, so those who have a desire can develop it, but so it removes all folders that begin with ".".